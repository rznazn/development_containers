use "docker build -t cplusserver ." to build the image and "docker-compose up" to run.

use your browser to open jupyter notebook at localhost:8888

grab the token from the prompt where you are running docker-compose and use it to log into and set password for jupyter notebook.

remember your password. it will be saved as long as the container isn't rebuilt.


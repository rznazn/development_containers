use "docker build -t cplusserver ." to build the image and "docker-compose up" to run.

use your browser to open code server at localhost:8443

login with "password" which is set in the docker-compose.yml file.

the same password is set as the sudo password to use in the code server terminal window.

cd to the helloworld directory and use "sh _build.sh" to build and run 